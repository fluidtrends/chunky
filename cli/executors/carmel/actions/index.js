const logout = require('./logout')
const login = require('./login')
const register = require('./register')
const hello = require('./hello')
const create = require('./create')

module.exports = { login, logout, register, hello, create }
